Arrow() => (
   MsgBox("1"), MsgBox("2"), MsgBox("3") ;You don't have to write the whole arrow function on a single line, since everything inside of brackets is being seen by the compiler as if it's written on a single line
)


Arrow()